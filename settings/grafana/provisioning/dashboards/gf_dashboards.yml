apiVersion: 1

providers:
  - name: pickple-dashboard # A uniquely identifiable name for the provider
    folder: pickple # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards